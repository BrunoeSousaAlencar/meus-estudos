algoritmo "sequênciadefibonacci"
var
     a, b, c, d, i, f: inteiro
inicio
      a <- 0
      b <- 0
      c <- 1
      escreva(a)
      escreva(a +1)
      enquanto (a < 13) faca
         a <- a + 1

      d <- b + c
        escreva(d)
      b <- c
      c <- d
      fimenquanto
fimalgoritmo